<?xml version="1.0" encoding="utf-8"?>
<!-- res/layout/activity_splash.xml -->
<!--
    WhatsApp-style splash screen for SPMODS.
    Background: #FFFFFF  |  Accent: #00A884 (WhatsApp green)
    Center: app logo + app name
    Bottom: "from SPMODS" text just like WhatsApp shows "from Meta"
-->
<RelativeLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@color/black">

    <!-- ── Center group: logo + app name ── -->
    <LinearLayout
        android:id="@+id/splash_center"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_centerInParent="true"
        android:orientation="vertical"
        android:gravity="center">

        <!-- App icon / logo -->
        <ImageView
            android:id="@+id/splash_logo"
            android:layout_width="96dp"
            android:layout_height="96dp"
            android:src="@drawable/ic_launcher"
            android:scaleType="fitCenter"
            android:contentDescription="@string/app_name" />

    </LinearLayout>

    <!-- ── Bottom section: "from SPMODS" ── -->
        <LinearLayout
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignParentBottom="true"
        android:layout_centerHorizontal="true"
        android:layout_marginBottom="48dp"
        android:orientation="vertical"
        android:gravity="center">

        <TextView
            android:id="@+id/splash_froms"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="from"
            android:textColor="#888888"
            android:textSize="14sp"
            android:letterSpacing="0.05" />

        <TextView
            android:id="@+id/splash_from"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="SPMODS"
            android:textColor="#FFFFFFFF"
            android:textSize="22sp"
            android:textStyle="bold"
            android:layout_marginTop="4dp" />

    </LinearLayout>
    
</RelativeLayout>
